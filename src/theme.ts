import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#5068AF',
    },
    secondary: {
      main: '#000',
    },
    background: {
      default: '#000',
    },
    text: {
      primary: '#000814',
    },
  },
});

export default theme;